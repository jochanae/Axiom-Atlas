# VITE_API_URL is intentionally unset in development — relative /api/ calls
# route through the Replit proxy to the backend automatically.
# Only set this for production (Vercel, Netlify, etc.) pointing to the live backend.
